# Portal Web — Instalação das Dependências
# Execute este script na pasta raiz do monorepo após clonar/atualizar

cd apps/web
npm install tailwindcss postcss autoprefixer lucide-react date-fns recharts jspdf jspdf-autotable --legacy-peer-deps

# Ou se usar pnpm na raiz:
# cd ../.. && pnpm install
