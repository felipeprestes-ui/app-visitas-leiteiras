============================================================
PASSO A PASSO VISUAL - GITHUB.DEV
Tela de agora: voce clicou em "Novo Arquivo" e apareceu uma caixa
============================================================

PASSO 1 (agora):
Voce deve estar vendo uma caixa de texto com cursor piscando.
Isso e onde voce digita o nome do arquivo.

Digite EXATAMENTE isso aqui:

apps/web/package.json

Depois aperte ENTER.

Isso vai criar o arquivo package.json dentro da pasta apps/web.
Se aparecer perguntando se quer criar a pasta, clique em SIM/CREATE.

============================================================
PASSO 2:
O arquivo package.json esta aberto. A tela esta em branco ou com algo.
Deixe o cursor no inicio do arquivo.

Cole o texto abaixo inteiro (Ctrl+C aqui, Ctrl+V no github.dev):

{"name":"crv-lagoa-portal","private":true,"version":"0.1.0","scripts":{"dev":"next dev","build":"next build","start":"next start","typecheck":"tsc --noEmit"},"dependencies":{"date-fns":"^3.6.0","jspdf":"^2.5.1","jspdf-autotable":"^3.8.2","lucide-react":"^0.400.0","next":"^14.2.0","react":"^18.3.1","react-dom":"^18.3.1","recharts":"^2.12.7"},"devDependencies":{"@types/node":"^20","@types/react":"^18","@types/react-dom":"^18","autoprefixer":"^10.4.19","postcss":"^8.4.38","tailwindcss":"^3.4.4","typescript":"^5"}}

============================================================
PASSO 3:
Aperte Ctrl + S
O arquivo foi salvo. Voce deve ver um pontinho ao lado do nome sumir.

============================================================
PASSO 4:
Agora cria outro arquivo:
Aperte Ctrl + N  (novo arquivo)

Digite:
apps/web/next.config.js

Aperte ENTER.

Cole isso:

/** @type {import('next').NextConfig} */
const path = require('path');
const nextConfig = {
  output: 'export',
  distDir: 'dist',
  trailingSlash: true,
  reactStrictMode: true,
  images: { unoptimized: true },
  webpack(config) {
    config.resolve.alias['@'] = path.join(__dirname, 'src');
    return config;
  },
};
module.exports = nextConfig;

Aperte Ctrl + S

============================================================
PASSO 5:
Ctrl + N

Digite:
apps/web/tsconfig.json

Cole:

{"compilerOptions":{"jsx":"preserve","lib":["dom","dom.iterable","es2021"],"allowJs":false,"noEmit":true,"incremental":true,"plugins":[{"name":"next"}],"paths":{"@/*":["./src/*"]}},"include":["src","next-env.d.ts",".next/types/**/*.ts"],"exclude":["node_modules"]}

Ctrl + S

============================================================
PASSO 6:
Ctrl + N

Digite:
apps/web/postcss.config.js

Cole:

module.exports = { plugins: { tailwindcss: {}, autoprefixer: {} } };

Ctrl + S

============================================================
PASSO 7:
Ctrl + N

Digite:
apps/web/tailwind.config.js

Cole:

/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/pages/**/*.{js,ts,jsx,tsx,mdx}',
    './src/components/**/*.{js,ts,jsx,tsx,mdx}',
    './src/app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        primary: { DEFAULT: '#1a3c7a', dark: '#0d1f4a', light: '#eef2fb' },
        gold: { DEFAULT: '#d4a843', dark: '#b8912e', light: '#fdf4dc' },
        border: '#c5d0e8',
        muted: '#4a5c82',
      },
    },
  },
  plugins: [],
};

Ctrl + S

============================================================
PASSO 8:
Ctrl + N

Digite:
apps/web/next-env.d.ts

Cole:

/// <reference types="next" />
/// <reference types="next/image-types/global" />

Ctrl + S

============================================================
PASSO 9:
Ctrl + N

Digite:
apps/web/.env.local

Cole:

NEXT_PUBLIC_SUPABASE_URL=https://egmradntktqzpuedxqhg.supabase.co/rest/v1
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImVnbXJhZG50a3RxenB1ZWR4cWhnIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Nzk5MTcwMTMsImV4cCI6MjA5NTQ5MzAxM30.f2mnogv5_EHVgEU-wUktMfVn7i28sf_vLhl1Baimtnk

Ctrl + S

============================================================
PASSO 10:
Ctrl + N

Digite:
apps/web/.gitignore

Cole:

/.next/
/dist/
/out/
node_modules/
*.log
.vercel/
.vscode/
.idea/
.DS_Store
Thumbs.db

Ctrl + S

============================================================
PASSO 11:
Ctrl + N

Digite:
.github/workflows/deploy-web.yml

Cole:

name: Build & Deploy Web Portal

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: apps/web/package.json
      - name: Install & Build
        working-directory: apps/web
        run: |
          npm install
          npm run build
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: apps/web/dist

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

Ctrl + S

============================================================
PARABENS! Os 11 arquivos de configuracao estao prontos.

AGORA FECHE O BLOCO DE NOTAS ou minimize.
No github.dev, va no icone da ESQUERDA que parece um ramo de arvore
com circulos (SOURCE CONTROL / CONTROLE DE CODIGO FONTE).

Digite a mensagem:
feat: adiciona portal web gestor

Clica no botao COMMIT.

Depois ative o GitHub Pages:
- Va no navegador em: github.com/felipeprestes-ui/app-visitas-leiteiras/settings/pages
- Em "Source" escolha "GitHub Actions"

Pronto! O site vai subir em 2-3 minutos em:
https://felipeprestes-ui.github.io/app-visitas-leiteiras/

============================================================
